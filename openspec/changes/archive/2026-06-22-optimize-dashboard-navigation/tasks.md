## 1. 導覽結構與視覺

- [x] 1.1 依照 **Editorial masthead with existing toolbar compatibility** 與 **Three destination navigation with current page state** 實作需求 **Dashboard branded navigation**、**Current page and keyboard navigation state**、**Dashboard workflow remains unchanged**：將 `dashboard.html` 的 `#toolbar.toolbar` 改為保留相容 class/id 的語意化 header，加入 CNA GRAPHICS／圖卡工作台品牌，以及指向 `dashboard.html`、`index.html`、`tutorial.html` 的三個導覽連結；Dashboard 連結須有 `.is-current` 與 `aria-current="page"`，並保持既有七個表單 id 不變。驗證方式：以 HTML 內容檢查確認 header/nav、三個 href、ARIA 屬性、品牌 class 與 `sheet-select`、`sheet-url`、`btn-connect`、`tab-select`、`template-select`、`btn-open`、`dash-status` 全部存在。
- [x] 1.2 依照 **Dashboard-scoped responsive navigation** 與 **Editorial masthead with existing toolbar compatibility** 實作需求 **Responsive dashboard navigation** 與 **Current page and keyboard navigation state**：在 `src/styles/dashboard.css` 建立深海軍藍、紅色 3px 細線的 72px 編輯台式 masthead，為 current／hover／focus-visible 狀態提供可辨識樣式與至少 44px 觸控高度；在 `max-width: 640px` 隱藏品牌副標、壓縮間距並讓 nav 可水平捲動而不換行，同步調整 dashboard 上方 padding。驗證方式：檢查所有新增 selector 均以 `.dashboard-toolbar` 為 scope，並在 1400px、640px、320px 三種 viewport 確認導覽無重疊、無多列、主內容未被遮住且三個目的地可觸達；若 Browser 介面不可用，改以 CSS 契約檢查確認單列 flex、`min-width: 0`、`overflow-x: auto`、`white-space: nowrap`、44px 觸控高度、mobile breakpoint 與主內容 offset 全部存在。

## 2. 驗證

- [x] 2.1 驗證 **Dashboard branded navigation**、**Current page and keyboard navigation state**、**Responsive dashboard navigation**、**Dashboard workflow remains unchanged**：用鍵盤 Tab 依序巡覽品牌與三個入口，確認 focus-visible 清楚且 Dashboard 持續顯示目前頁狀態；確認 Sheets 連結、分頁與版型選單、開啟按鈕仍沿用 `src/dashboard.js` 且 console 無未捕捉錯誤；若 Browser 介面不可用，改以 HTML parser、CSS 契約檢查與 `node --check src/dashboard.js` 驗證相同靜態契約；最後執行 `spectra analyze optimize-dashboard-navigation --json` 與 `spectra validate optimize-dashboard-navigation` 並確認通過。
